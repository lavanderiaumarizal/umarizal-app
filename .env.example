# Variáveis de ambiente do app (Expo — prefixo obrigatório EXPO_PUBLIC_)
# Copie este arquivo para .env e ajuste se necessário (o .env não vai para o git).

# URL da API (padrão: produção). Use para apontar para staging/localhost em dev.
EXPO_PUBLIC_API_URL=https://api.lavanderiaumarizal.com.br/api
